DATABASE_URL="postgresql://postgres:password@localhost:5432/pipeline"  #postgresUrl 
#note: if password contains the special characters we have to use the encode
## ! => %21, # => %23, $ => %24, % => %25, & => %26, ' => %27, ( => %28, ) => %29, * => %2A, + => %2B, , => %2C, / => %2F, : => %3A, ; => %3B, = => %3D, ? => %3F, @ => %40, [ => %5B, ] => %5D, 

DEFAULT_API_KEY=""   #Google API key

DEAULT_MODEL=""

AI_PROVIDER="nvidia"

NVIDIA_KEY=""    #Nvidia API Key
NVIDIA_MODEL=""

GROQ_KEY=""    #Groq API Key
GROQ_MODEL=""

HFACE_KEY=""    #HuggingFace API Key
HFACE_MODEL=""

# AI_TIMEOUT=""
# AI_RETRIES=""
